<Window x:Class="NanairoWorkReportTool.OutputCompletedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="出力完了" Width="620" Height="260" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="#F3F4F6">
    <Border Margin="14" Padding="18" Background="White" CornerRadius="10" BorderBrush="#D1D5DB" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="✓ 出力が完了しました" FontSize="22" FontWeight="Bold" Foreground="#15803D"/>
            <TextBlock Grid.Row="1" Text="{Binding ExcelPath}" Margin="0,14,0,0" TextWrapping="Wrap"/>
            <TextBlock Grid.Row="2" Text="{Binding PdfPath}" Margin="0,4,0,0" TextWrapping="Wrap"/>
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Excelを開く" Click="OpenExcel_Click"/>
                <Button x:Name="OpenPdfButton" Content="PDFを開く" Click="OpenPdf_Click"/>
                <Button Content="保存先を開く" Click="OpenFolder_Click"/>
                <Button Content="閉じる" IsDefault="True" Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
